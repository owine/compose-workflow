{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Shared Renovate preset for Docker Compose repositories",
  "extends": [
    "config:best-practices",
    ":semanticPrefixFixDepsChoreOthers"
  ],
  "timezone": "America/Chicago",
  "prHourlyLimit": 0,
  "prConcurrentLimit": 0,
  "minimumReleaseAgeBehaviour": "timestamp-optional",
  "commitMessageTopic": "{{depName}}",
  "commitMessageExtra": "{{#if isDigest}}digest to {{newDigestShort}}{{else}}to {{#if isMajor}}{{prettyNewMajor}}{{else}}{{prettyNewVersion}}{{/if}}{{/if}}",
  "vulnerabilityAlerts": {
    "enabled": true,
    "labels": [
      "security",
      "dependencies"
    ],
    "automerge": true
  },
  "packageRules": [
    {
      "groupName": "All dependencies",
      "groupSlug": "all-deps",
      "matchUpdateTypes": [
        "minor",
        "patch",
        "pin",
        "digest"
      ],
      "automerge": true,
      "minimumReleaseAge": "1 hour"
    },
    {
      "groupName": "Major GitHub Actions",
      "groupSlug": "all-deps-major-actions",
      "matchManagers": [
        "github-actions"
      ],
      "matchUpdateTypes": [
        "major"
      ],
      "automerge": true,
      "minimumReleaseAge": "3 days"
    },
    {
      "groupName": "Major Docker images",
      "groupSlug": "all-deps-major-docker",
      "matchManagers": [
        "docker-compose",
        "dockerfile"
      ],
      "matchUpdateTypes": [
        "major"
      ],
      "automerge": false,
      "minimumReleaseAge": "3 days"
    }
  ]
}
